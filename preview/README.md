# Preview

`light.png` / `dark.png` are Skin Center preview captures.

For local interactive preview, generate `lib/client.js` first:

```sh
node scripts/build-fallback.mjs
```

Then open:

```text
demo.html?theme=light
demo.html?theme=dark
```
